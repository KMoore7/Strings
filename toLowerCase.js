// .toLowerCase() — Lower cases entire string
// The toLowerCase() function returns a string with all lower case letters.

let str = 'This has BIG LETTERS. They want to all be small.';

console.log(str.toLowerCase()); // "this has big letters. they want all to be small."
